services:
  - type: web
    name: AATraders-bot
    env: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    runtime: python-3.11  # Change this to 3.10 or 3.11
